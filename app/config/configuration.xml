<?xml version="1.0" encoding="utf-8" ?>
<configuration>

    <database var="hostname" value="localhost" />
    <database var="dbname" value="perso_blog" />
    <database var="username" value="root" />
    <database var="password" value="" />

    <!-- Default SQL type for creating database connections -->
    <database var="default_sql_type" value="mysql" />

    <!-- Default path where pictures of posts are stocked -->
    <path var="default_picture_posts_path" value="../public/image/posts" />
</configuration>